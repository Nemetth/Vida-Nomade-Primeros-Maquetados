/* "use strict"

var mediaquery = window.matchMedia("(max-width: 1023px)");
function funcion() {
  var div = document.querySelector(".grande");
  if (mediaquery.matches) {

  } else {
    div.classList.add("column-reverse");
  }
}
mediaquery.addListener(funcion) */
